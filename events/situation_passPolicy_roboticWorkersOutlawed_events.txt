namespace = situation_passPolicy_roboticWorkersOutlawed
country_event = {
	id = situation_passPolicy_roboticWorkersOutlawed.1
	title = situation_passPolicy_roboticWorkersOutlawed.1.name
	desc = situation_passPolicy_roboticWorkersOutlawed.1.desc
	picture = GFX_evt_organic_oppression
	
	show_sound = {
		sound = advisor_notification_planet_in_revolt
		sound_is_advisor = yes
	}
	
	location = root
	is_triggered_only = yes
	situation = event_target:new_situation

	immediate = {
		owner = {
			start_situation = {
				type = situation_passPolicy_roboticWorkersOutlawed
				target = root
				effect = {
					save_event_target_as = new_situation
				}
			}
		}
	}

	option = {
		name = passPolicy_ack

		tooltip = {
			owner = {
				start_situation = {
					type = situation_passPolicy_roboticWorkersOutlawed
					target = root
				}
			}
		}
	}
}

# Determine advance fail or stall
situation_event = {
	id = situation_passPolicy_roboticWorkersOutlawed.2	
	is_triggered_only = yes
	hide_window = yes

	immediate = {
		if = {
			limit = {
				NOT = {
					is_variable_set = setback_count
				}
			}
			set_variable = { which = setback_count value = 0}
		}
		if = {
			limit = {
				NOT = {
					is_variable_set = swayed_to_support
				}
			}
			set_variable = { which = swayed_to_support value = 0}
		}
		if = {
			limit = {
				NOT = {
					is_variable_set = swayed_to_detract
				}
			}
			set_variable = { which = swayed_to_detract value = 0}
		}
		set_variable = { which = faction_support value = 0}
		set_variable = { which = faction_detraction value = 0}
	
		OWNER = {
			every_pop_faction = {
				
				if = {
					limit = {
						is_pop_faction_type = technologist
					}
					export_trigger_value_to_variable = { trigger = support variable = technologist_support }
					multiply_variable = {which = technologist_support value = 100}
					ROOT = {
						change_variable = { which = faction_support value = PREV.technologist_support }		
					}
				}
				
				if = {
					limit = {
						is_pop_faction_type = prosperity
					}
					export_trigger_value_to_variable = { trigger = support variable = prosperity_support }
					multiply_variable = {which = prosperity_support value = 100}
					ROOT = {
						change_variable = { which = faction_support value = PREV.prosperity_support }		
					}
				}
				
				
				if = {
					limit = {
						is_pop_faction_type = traditionalist
					}
					export_trigger_value_to_variable = { trigger = support variable = traditionalist_support }
					multiply_variable = {which = traditionalist_support value = 100}
					ROOT = {
						change_variable = { which = faction_detraction value = PREV.traditionalist_support }	
					}
				}
				
			}
		}

		set_variable = {which = faction_ambivalence value = 100}
		subtract_variable = { which = faction_ambivalence value = faction_support }
		subtract_variable = { which = faction_ambivalence value = faction_detraction }
		
		# log = "PRE MATH: roboticWorkersOutlawed support: [THIS.faction_support]+[THIS.swayed_to_support] detraction: [THIS.faction_detraction]+[THIS.swayed_to_detract]"	

		# If swayed is positive, give it support, if negative, give it to detraction
		
		change_variable = { which = faction_support value = swayed_to_support }
		change_variable = { which = faction_detraction value = swayed_to_detract }
		

		# Clamp variables
		if = {
			limit = {
				check_variable = { which = faction_support value > 100}
			}
			set_variable = { which = faction_support value = 100}
		}
		if = {
			limit = {
				check_variable = { which = faction_support value < 0}
			}
			set_variable = { which = faction_support value = 0}
		}
		
		if = {
			limit = {
				check_variable = { which = faction_detraction value > 100}
			}
			set_variable = { which = faction_detraction value = 100}
		}
		if = {
			limit = {
				check_variable = { which = faction_detraction value < 0}
			}
			set_variable = { which = faction_detraction value = 0}
		}

		set_variable = {which = faction_ambivalence value = 100}
		subtract_variable = { which = faction_ambivalence value = faction_support }
		subtract_variable = { which = faction_ambivalence value = faction_detraction }

		if = {
			limit = {
				check_variable = { which = faction_ambivalence value > 100}
			}
			set_variable = { which = faction_ambivalence value = 100}
		}
		if = {
			limit = {
				check_variable = { which = faction_ambivalence value < 0}
			}
			set_variable = { which = faction_ambivalence value = 0}
		}


		ceiling_variable = faction_support
		ceiling_variable = faction_detraction
		ceiling_variable = faction_ambivalence
		# log = "POST MATH: roboticWorkersOutlawed support: [THIS.faction_support] detraction: [THIS.faction_detraction] ambivalence: [THIS.faction_ambivalence]"	
	}
}
# Decide whether or not to roll
situation_event = {
	id = situation_passPolicy_roboticWorkersOutlawed.3
	is_triggered_only = yes
	hide_window = yes
	
	immediate = {
		# Update all values
		situation_event = {
			id = situation_passPolicy_roboticWorkersOutlawed.2	
		}
		set_variable_to_random_value = { which = advancement_roll min = 0 max = 100 rounded = yes}
		# log = "Rolled: [THIS.advancement_roll] [THIS.faction_support]:[THIS.faction_detraction]:[THIS.faction_ambivalence]"
		if = {
			limit = {
				check_variable = { which = advancement_roll value < faction_support  }
			}
			# Advance!
			situation_event = { id = situation_passPolicy_roboticWorkersOutlawed.4}
		}
		else = {
			subtract_variable = { which = advancement_roll value = faction_support }
			if = {
				limit = {
					check_variable = { which = advancement_roll value < faction_detraction }
				}
				# Fail!
				situation_event = {id = situation_passPolicy_roboticWorkersOutlawed.5}
			}
			else = {
				# Ambivalence!
				situation_event = {id = situation_passPolicy_roboticWorkersOutlawed.6}
			}
		}	
	}
}

# Advance
situation_event = {
	id = situation_passPolicy_roboticWorkersOutlawed.4
	is_triggered_only = yes
	hide_window = yes
	immediate = {
		# log = "roboticWorkersOutlawed success!"
		add_situation_progress = 34
	}
}
# Fail
situation_event = {
	id = situation_passPolicy_roboticWorkersOutlawed.5
	is_triggered_only = yes
	hide_window = yes
	immediate = {
		change_variable = { which = setback_count value = 1 }
		# log = "roboticWorkersOutlawed fail! [this.setback_count]/3"
		if = {
			limit = { check_variable = { which = setback_count value = 3}}
			# log = "roboticWorkersOutlawed failed due to setbacks [this.setback_count]/3!"
			situation_event = { id = situation_passPolicy_roboticWorkersOutlawed.9}
		}
	}
}

# Ambivalent
situation_event = {
	id = situation_passPolicy_roboticWorkersOutlawed.6
	is_triggered_only = yes
	hide_window = yes
	immediate = {
		if = {
			limit = {
				check_variable = { which = faction_ambivalence value > 10}
			}
			set_variable_to_random_value = { which = sway_roll min = 0 max = 100 rounded = yes}
			if = {
				limit = {
					check_variable = { which = sway_roll value > 50 }
				}
				# log = "roboticWorkersOutlawed ambivalent! Rolled [THIS.sway_roll]. Giving 10 ambivalence support."
				change_variable = { which = swayed_to_support value = 10}
			}
			else = {
				# log = "roboticWorkersOutlawed ambivalent! Rolled [THIS.sway_roll]. Giving 10 ambivalence detraction."
				change_variable = { which = swayed_to_detract value = 10}
			}
			situation_event = {id = situation_passPolicy_roboticWorkersOutlawed.2 }
		}
		else = {
			# log = "roboticWorkersOutlawed ambivalent! No ambivalence to give at [THIS.faction_ambivalence]."
		}
	}
}

# Success
situation_event = {
	id = situation_passPolicy_roboticWorkersOutlawed.7
	title = situation_passPolicy_roboticWorkersOutlawed.7.name
	desc = situation_passPolicy_roboticWorkersOutlawed.7.desc
	picture = GFX_evt_organic_oppression
	
	is_triggered_only = yes
	immediate = {
		owner = {
			set_policy = { 
				policy = artificial_intelligence_policy 
				option = ai_outlawed 
				cooldown = yes 
			}
		}
		destroy_situation = this
	}
	option = {
		name = situation_passPolicy_roboticWorkersOutlawed.7.a
	}
}
# Failure due to abort
situation_event = {
	id = situation_passPolicy_roboticWorkersOutlawed.8
	title = situation_passPolicy_roboticWorkersOutlawed.8.name
	desc = situation_passPolicy_roboticWorkersOutlawed.8.desc
	picture = GFX_evt_organic_oppression
	
	is_triggered_only = yes

	immediate = {
		destroy_situation = this
	}
	option = {
		name = situation_passPolicy_roboticWorkersOutlawed.8.a
	}
}
# Failure due to setbacks
situation_event = {
	id = situation_passPolicy_roboticWorkersOutlawed.9
	title = situation_passPolicy_roboticWorkersOutlawed.9.name
	desc = situation_passPolicy_roboticWorkersOutlawed.9.desc
	picture = GFX_evt_organic_oppression
	
	is_triggered_only = yes
	immediate = {
		destroy_situation = this
	}
	
	option = {
		name = situation_passPolicy_roboticWorkersOutlawed.9.a
	}
}