
calculate_first_contact_proactive_movement_support_and_radicalism_from_factions = {
	every_pop_faction = {
		
		if = {
			limit = {
				OR = {
					
					
				}
				
				NAND = {
					is_pop_faction_type = armedIsolationism
					ROOT = { has_policy_flag = first_contact_cautious }
				}
				
			}
		
			export_trigger_value_to_variable = { trigger = support variable = first_contact_proactive_support }
			multiply_variable = {which = first_contact_proactive_support value = 100}
			# first_contact_proactive_support is 0 to 100
			
			export_trigger_value_to_variable = { trigger = faction_approval variable = first_contact_proactive_approval }
			# first_contact_proactive_approval is 0 to 1

			ROOT = {
				change_variable = { which = $SUPPORT$ value = PREV.first_contact_proactive_support }
				
				set_variable = { which = first_contact_proactive_radicalism value = 1 }
				subtract_variable = { which = first_contact_proactive_radicalism value = PREV.first_contact_proactive_approval }
				
				multiply_variable = { which = first_contact_proactive_radicalism value = PREV.first_contact_proactive_support }

				change_variable = { which = $RADICALISM$ value = first_contact_proactive_radicalism }
			}
		}
	}
}

calculate_first_contact_proactive_movement_support_and_radical_from_numbers = {
	set_variable = { which = first_contact_proactive_population value = 0}
	set_variable = { which = first_contact_proactive_supporting_population value = 0}
	set_variable = { which = first_contact_proactive_radical_population value = 0}
	
	every_owned_pop = {
		ROOT = {
			change_variable = { which = first_contact_proactive_population value = 1}
		}
		
		if = {
			limit = {
				OR = {
					
				}
			}
			# A perfectly happy pop has happiness 1, and unhappy 0. An perfectly unhappy pop in thise case provides 1 radicalism support whereas his happy cousin contributes none.
			export_modifier_to_variable = { modifier = pop_happiness variable = pop_happiness }
			ROOT = {
				set_variable = {which = pop_first_contact_proactive_radicalism value = 1}
				subtract_variable = { which = pop_first_contact_proactive_radicalism value = PREV.pop_happiness }

				change_variable = { which = first_contact_proactive_supporting_population value = 1 }
				change_variable = { which = first_contact_proactive_radical_population value = pop_first_contact_proactive_radicalism }
			}
		}
	}
	
	set_variable = { which = $SUPPORT$ value = first_contact_proactive_supporting_population}
	divide_variable = { which = $SUPPORT$ value = first_contact_proactive_population }
	multiply_variable = {which = $SUPPORT$ value = 100}

	set_variable = { which = $RADICALISM$ value = first_contact_proactive_radical_population}
	divide_variable = { which = $RADICALISM$ value = first_contact_proactive_population }
	multiply_variable = {which = $RADICALISM$ value = 100}
}