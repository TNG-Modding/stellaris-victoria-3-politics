pass_purgeNotAllowed = {
	length = 0
	icon = "GFX_edict_type_policy"

	potential = {
		NOT = { has_policy_flag = purge_not_allowed }
		NOT = { has_ethic = ethic_gestalt_consciousness } NOT = { has_valid_civic = civic_fanatic_purifiers }
	}

	allow = {
		custom_tooltip = {
			fail_text = no_support_to_pass
			AND = {
				is_variable_set = movement_purgeNotAllowed_faction_support
			}
			check_variable = { which = movement_purgeNotAllowed_faction_support value > 0 }
		}
		custom_tooltip = {
			fail_text = already_passing_policy
			NOT = {has_country_flag = enacting_policy}
		}
		# can_change_policy = purge
	}
	effect = {
		country_event = { id = situation_passPolicy_purgeNotAllowed.1 }
	}

	ai_weight = {
		# weight = 500
	}
}